# 读取对象PocData并形成数据包结构,使其能正常发送
from core.bin.Oreadjson import PocData
import requests

def getFingerprint(PocData):
    """
    读取PocData对象并发送请求获取指纹信息
    :param url: Url对象
    :return: 指纹信息
    """
    # 获取PocData对象中的数据

# 被动指纹匹配
def passiveFingerprintMatching(response,responseForFingerprint):
    """
    被动指纹匹配
    :param PocData: PocData对象
    :return: 匹配结果
    """
    
    


# 加载全部fingerprint,与响应进行匹配
# def Fingerprintmatching(PocData):
    